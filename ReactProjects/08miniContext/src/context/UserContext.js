import React from 'react';

//  Create a context object
const UserContext = React.createContext();

export default UserContext;
// we have to context provider which wil be used by the components to access the data
